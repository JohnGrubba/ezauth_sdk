{
  "name": "ezauth_js",
  "module": "index.ts",
  "type": "commonjs",
  "scripts": {
    "build": "bun build --entrypoints ./src/index.ts --outdir ./out --target browser --minify",
    "dev": "bun build --entrypoints ./src/index.ts --outdir ./out --target browser --minify --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}